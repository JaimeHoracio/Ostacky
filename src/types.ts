/** Rutas dentro de un directorio .opencode */
export interface OpenCodePaths {
  root: string;
  agents: string;
  commands: string;
  plugins: string;
  skills: string;
  mcp: string;
  tools: string;
}
