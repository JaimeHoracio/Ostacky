name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Lint & Check
        run: bun run build

      - name: Run tests
        run: bun test

      - name: Check skill sync
        run: bun run check:skills

      - name: Check manifest hashes
        run: bun run hash:check
